<div class='nav {{#if isHorizontal}}nav_horizontal
        {{else}}nav_inLine{{/if}}'>
    {{#each list}}
        <a href='{{url}}' class='nav_link'>{{text}}</a>
        {{else}}
        <a href='{{path}}' class='nav_link'>{{name}}</a>
    {{/each}}
</div>